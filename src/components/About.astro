---
// Функция для расчёта стажа от указанной даты
function calculateExperience(startDate: string): string {
  const start = new Date(startDate);
  const now = new Date();
  
  const diffTime = Math.abs(now.getTime() - start.getTime());
  const diffYears = Math.floor(diffTime / (1000 * 60 * 60 * 24 * 365.25));
  const diffMonths = Math.floor((diffTime % (1000 * 60 * 60 * 24 * 365.25)) / (1000 * 60 * 60 * 24 * 30.44));
  
  if (diffYears === 0) {
    return `${diffMonths}-месячным опытом`;
  } else if (diffMonths === 0) {
    return `${diffYears}-летним опытом`;
  } else {
    return `${diffYears}-летним опытом`;
  }
}

const experience = calculateExperience('2016-01-01');

const stats = [
  { 
    icon: "M7 21a4 4 0 01-4-4V5a2 2 0 012-2h4a2 2 0 012 2v12a4 4 0 01-4 4zM21 5a2 2 0 00-2-2h-4a2 2 0 00-2 2v12a4 4 0 004 4 4 4 0 004-4V5z",
    title: "ТОЧНОСТЬ", 
    desc: "Миллиметровая точность в каждом чертеже", 
    number: "100%" 
  },
  { 
    icon: "M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z",
    title: "ДЕТАЛИ", 
    desc: "Внимание к каждой детали проекта", 
    number: "∞" 
  },
  { 
    icon: "M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6",
    title: "ФУНКЦИЯ", 
    desc: "Практичность и удобство использования", 
    number: "24/7" 
  },
  { 
    icon: "M9 12l2 2 4-4M7.835 4.697a3.42 3.42 0 001.946-.806 3.42 3.42 0 014.438 0 3.42 3.42 0 001.946.806 3.42 3.42 0 013.138 3.138 3.42 3.42 0 00.806 1.946 3.42 3.42 0 010 4.438 3.42 3.42 0 00-.806 1.946 3.42 3.42 0 01-3.138 3.138 3.42 3.42 0 00-1.946.806 3.42 3.42 0 01-4.438 0 3.42 3.42 0 00-1.946-.806 3.42 3.42 0 01-3.138-3.138 3.42 3.42 0 00-.806-1.946 3.42 3.42 0 010-4.438 3.42 3.42 0 00.806-1.946 3.42 3.42 0 013.138-3.138z",
    title: "КАЧЕСТВО", 
    desc: "Высокие стандарты исполнения", 
    number: "A+" 
  },
];
---

<section id="about" class="py-24 relative overflow-hidden">
  <!-- Grid background -->
  <div class="absolute inset-0 opacity-5">
    <div
      class="h-full w-full"
      style="background-image: linear-gradient(to right, #000 1px, transparent 1px), linear-gradient(to bottom, #000 1px, transparent 1px); background-size: 50px 50px; background-position: center;"
    >
    </div>
  </div>

  <!-- Floating elements -->
  <div class="absolute inset-0 pointer-events-none overflow-hidden">
    <div
      class="absolute top-20 left-20 w-2 h-2 bg-gray-400 rounded-full animate-bounce"
      style="animation-duration: 2s;"
    >
    </div>
    <div
      class="absolute top-40 right-32 w-1 h-1 bg-gray-500 rounded-full animate-pulse"
    >
    </div>
    <div
      class="absolute bottom-32 left-32 w-3 h-3 border border-gray-400 animate-spin"
      style="animation-duration: 8s;"
    >
    </div>
    <div
      class="absolute bottom-20 right-20 w-2 h-2 bg-gray-300 animate-pulse"
      style="animation-delay: 1s;"
    >
    </div>
  </div>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="grid lg:grid-cols-2 gap-16 items-center">
      <!-- Text content -->
      <div class="about-text opacity-0">
        <div class="relative">
          <h2 class="text-4xl md:text-5xl font-bold tracking-wider mb-8 bg-gradient-to-r from-gray-900 to-gray-600 bg-clip-text text-transparent">
            О ДИЗАЙНЕРЕ
          </h2>
          <div class="absolute -left-4 top-0 w-1 h-16 bg-gradient-to-b from-black to-gray-400"></div>
        </div>

        <div class="space-y-6 text-gray-700">
          <p class="text-lg leading-relaxed about-paragraph opacity-0 stagger-1">
            Профессиональный дизайнер интерьера с <span class="font-bold text-black">{experience}</span>
            создания уникальных пространств. Специализируюсь на жилых и коммерческих проектах.
          </p>
          <p class="leading-relaxed about-paragraph opacity-0 stagger-2">
            Использую современные инструменты проектирования:
            <span class="font-bold text-black">AutoCAD, 3ds Max, SketchUp</span>, что позволяет создавать точные
            технические чертежи и реалистичные визуализации.
          </p>
          <p class="leading-relaxed about-paragraph opacity-0 stagger-3">
            Мой подход основан на <span class="font-bold text-black">функциональности, эргономике</span> и
            индивидуальных потребностях клиента. Каждый проект - это уникальное решение, созданное с вниманием к
            деталям.
          </p>
        </div>
      </div>

      <!-- Stats grid -->
      <div class="grid grid-cols-2 gap-6 about-stats opacity-0">
        {stats.map((stat, index) => (
          <div class={`group relative bg-white/80 backdrop-blur-sm border border-gray-200/50 p-6 hover:shadow-2xl transition-all duration-500 cursor-pointer stat-card opacity-0 stagger-${index + 1}`}>
            <!-- Hover gradient overlay -->
            <div class="absolute inset-0 bg-gradient-to-br from-gray-50 to-white opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>

            <div class="relative z-10">
              <div class="flex items-center justify-between mb-4">
                <svg class="h-8 w-8 group-hover:scale-110 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d={stat.icon}></path>
                </svg>
                <span class="text-2xl font-bold text-gray-400 group-hover:text-black transition-colors duration-300">
                  {stat.number}
                </span>
              </div>
              <h3 class="font-bold mb-2 tracking-wide group-hover:text-black transition-colors duration-300">
                {stat.title}
              </h3>
              <p class="text-sm text-gray-600 leading-relaxed">{stat.desc}</p>
            </div>

            <!-- Animated border -->
            <div class="absolute inset-0 border-2 border-transparent group-hover:border-black/20 transition-all duration-300"></div>

            <!-- Corner accents -->
            <div class="absolute top-0 left-0 w-3 h-3 border-l-2 border-t-2 border-black opacity-0 group-hover:opacity-100 transition-opacity duration-300 -translate-x-1 -translate-y-1"></div>
            <div class="absolute bottom-0 right-0 w-3 h-3 border-r-2 border-b-2 border-black opacity-0 group-hover:opacity-100 transition-opacity duration-300 translate-x-1 translate-y-1"></div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<script>
  // Intersection Observer for animations
  const observerOptions = {
    threshold: 0.2,
    rootMargin: '0px 0px -50px 0px'
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const aboutText = entry.target.querySelector('.about-text');
        const aboutStats = entry.target.querySelector('.about-stats');
        const paragraphs = entry.target.querySelectorAll('.about-paragraph');
        const statCards = entry.target.querySelectorAll('.stat-card');

        aboutText?.classList.add('animate-slide-left');
        aboutStats?.classList.add('animate-slide-right');
        
        paragraphs.forEach(p => p.classList.add('animate-slide-up'));
        statCards.forEach(card => card.classList.add('animate-slide-up'));
      }
    });
  }, observerOptions);

  document.addEventListener('DOMContentLoaded', () => {
    const aboutSection = document.getElementById('about');
    if (aboutSection) {
      observer.observe(aboutSection);
    }
  });
</script>
